import "FungibleToken"
import "FungibleTokenMetadataViews"

import "IncrementSwapStack"

transaction(vaultIdentifier: String) {

    prepare(signer: auth(Storage, StorageCapabilities) &Account) {
        
    }
}